const count=document.getElementById('guestCount'),wrap=document.getElementById('guestWrap'),form=document.getElementById('rsvpForm');function guestCard(i){return `<section class="card guest-card"><div class="eyebrow">Guest ${i+1}</div><div class="field"><label>Full name</label><input required name="guest_${i}_name" placeholder="Enter guest name"></div><h3 class="serif">Please RSVP to each event</h3><div class="event-options">${['Welcome Party','Bingo','Pickleball','Wedding'].map(ev=>`<div class="event"><strong>${ev}</strong><div class="choice"><label><input required type="radio" name="guest_${i}_${ev}" value="yes" data-event="${ev}" data-guest="${i}">Yes</label><label><input required type="radio" name="guest_${i}_${ev}" value="no" data-event="${ev}" data-guest="${i}">No</label></div>${ev==='Bingo'?`<div class="field hidden" id="fact_${i}"><label>Your fun fact</label><textarea name="guest_${i}_fact" placeholder="Share one surprising or fun fact."></textarea></div>`:''}</div>`).join('')}</div></section>`}function render(){wrap.innerHTML=Array.from({length:+count.value},(_,i)=>guestCard(i)).join('');wrap.querySelectorAll('[data-event="Bingo"]').forEach(r=>r.addEventListener('change',()=>{const f=document.getElementById('fact_'+r.dataset.guest);f.classList.toggle('hidden',r.value!=='yes');f.querySelector('textarea').required=r.value==='yes';}));}count.addEventListener('change',render);render();form.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));const submissions=JSON.parse(localStorage.getItem('harrisonRSVPs')||'[]');submissions.push({...data,submittedAt:new Date().toISOString(),inviteOpenedBy:JSON.parse(localStorage.getItem('harrisonGuest')||'null')});localStorage.setItem('harrisonRSVPs',JSON.stringify(submissions));form.classList.add('hidden');document.getElementById('thanks').classList.remove('hidden');});

const overlay=document.getElementById('faqOverlay');document.querySelectorAll('.faq button').forEach(b=>b.addEventListener('click',e=>{e.stopPropagation();overlay.querySelector('h3').textContent=b.textContent;overlay.querySelector('p').textContent=b.dataset.answer;overlay.classList.remove('hidden');}));document.addEventListener('click',()=>overlay.classList.add('hidden'));overlay.addEventListener('click',e=>e.stopPropagation());

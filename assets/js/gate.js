const form=document.getElementById('gateForm');form.addEventListener('submit',e=>{e.preventDefault();const first=form.first.value.trim(),last=form.last.value.trim(),err=document.querySelector('.gate-error');if(!first||!last){err.textContent='Please enter both your first and last name.';return;}localStorage.setItem('harrisonGuest',JSON.stringify({first,last,openedAt:new Date().toISOString()}));location.href='home.html';});
